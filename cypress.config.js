module.exports = {
  e2e: {
    setupNodeEvents(on, config) {
      // Implemente os listeners de eventos do Node aqui
    },
    specPattern: 'cypress/e2e/**/*.{cy.js,cy.jsx,cy.ts,cy.tsx}', // Ajuste o caminho e as extensões de arquivo conforme necessário
  },
};
